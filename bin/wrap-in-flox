#!/bin/bash

set -e

if command -v flox &> /dev/null && [ -z "$FLOX_ENV" ]; then
# If `flox` available and #$FLOX_ENV is empty, activate the environment
    flox activate -- "$@"
else
    # Otherwise, run the command directly
    "$@"
fi
